version: "3.9"

services:
  chatops_bot:
    build: .
    container_name: chatops_bot
    ports:
      - "8000:8000"
    volumes:
      - ./scripts:/app/scripts
      - /var/run/docker.sock:/var/run/docker.sock  # allow Docker commands
    restart: always